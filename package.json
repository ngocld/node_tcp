{
  "name": "node_tcp",
  "version": "1.0.0",
  "description": "example network",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ngocld",
  "license": "ISC",
  "dependencies": {
    "sleep-promise": "^9.1.0"
  }
}
